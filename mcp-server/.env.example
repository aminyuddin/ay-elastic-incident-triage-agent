# Firebase Admin — use the same project as the Vite portal.
# Option A: Path to service account JSON (download from Firebase Console → Project settings → Service accounts)
GOOGLE_APPLICATION_CREDENTIALS=./path-to-service-account.json

# Option B: Paste the whole JSON as one line (e.g. in production secrets)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# MCP auth — required. Clients must send this value in a header (Kibana: add HTTP header in connector).
# Use Authorization: Bearer <this-value> or X-API-Key: <this-value>.
MCP_AUTH_SECRET=your-secret-here

# Optional: created_by value for new incidents (default: elastic-agent)
# MCP_CREATED_BY=elastic-agent

# Optional: port (default 3000)
# PORT=3000
